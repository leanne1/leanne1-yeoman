// import TestSubject from '';

describe('<%= projectName %>', () => {
	let testSubject,
		testOptionsStub;

	// beforeEach(() => {
	// 	testOptionsStub = {
	// 		el: null
	// 	};
	// 	testSubject = new TestSubject(testOptionsStub);
	// });

	// afterEach(() => {
 //        [].slice.call(document.querySelectorAll('.js-**')).forEach(el => {
 //            el.parentNode.removeChild(el);
 //        });
 //    });

	describe('when initialised', () => {	
		it('it should always pass this', ()=>{
			expect(true).toBeTruthy();
		});
	});
});
